import 'package:flutter/material.dart';

class ConditioningScreen extends StatelessWidget {
  const ConditioningScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Combat Conditioning')),
      body: const Center(child: Text('Tactical conditioning routines coming soon!')),
    );
  }
}